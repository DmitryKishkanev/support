import styled from '@emotion/styled';

const PokemonName = styled.p`
  color: var(--primary-color);
  font-weight: 600;
  font-size: 20px;
  text-transform: uppercase;
`;

const PokemonStatsItem = styled.li`
  color: var(--primary-color);
`;

export { PokemonName, PokemonStatsItem };
