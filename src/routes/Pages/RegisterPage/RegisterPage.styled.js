import styled from '@emotion/styled';

const RegisterPageBox = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  background-color: rgba(0, 0, 0, 0.7);
`;

export { RegisterPageBox };
